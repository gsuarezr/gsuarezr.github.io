---
interface Props {
  title?: string;
  className?: string;
}

const { title, className = '' } = Astro.props;
---

<div class={`bg-white dark:bg-gray-800 rounded-lg shadow-sm p-6 ${className}`}>
  {title && <h2 class="text-xl font-semibold mb-4">{title}</h2>}
  <slot />
</div>